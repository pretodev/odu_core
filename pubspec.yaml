name: odu_core
description: Core utilities for Flutter applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.13.2

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.5.2
  collection: ^1.19.1
  equatable: ^2.1.0
  # path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
  test: ^1.25.6
